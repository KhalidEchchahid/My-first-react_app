import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

ReactDOM.render(<App />, document.getElementById('root'));

/* khdina l method render mn l biblio ReactDOM
  w l function render katakhod 2 things (what , where)
  */

  // jsx 
/* f jsx gaa3 tags (hadok machi  html tags ==> XML) 
    kaytktbo fwst tag we7da */